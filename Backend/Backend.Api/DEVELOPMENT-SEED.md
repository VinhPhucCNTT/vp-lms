# Development seed credentials

The API seeds these accounts only when running in the `Development` environment.
The seeder is idempotent and creates one published course, one student enrollment, and one published module for the student flow.

Student login:

- Email: `dev.student@vp-lms.local`
- Password: `Student123!`

Instructor account:

- Email: `dev.instructor@vp-lms.local`
- Password: `Instructor123!`

These credentials are for local development only and must not be used as production credentials.
